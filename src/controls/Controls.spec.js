// Test away!
import React from "react";
import { render } from "@testing-library/react";
import Controls from "./Controls";

test("successfully renders the controls component", () => {
	render(<Controls />);
});
